export const COUNTER_IDS = [1, 2, 3, 4] as const;

/** Milestone total that triggers a celebration. */
export const GOAL = 100;

/** Maximum allowed step value (slider + presets + storage clamp). */
export const MAX_STEP = 25;

/** How long the goal message and confetti stay visible. */
export const CELEBRATION_MS = 3500;
